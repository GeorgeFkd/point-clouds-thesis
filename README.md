# point-clouds-thesis

Running the ./pointclouds project(uv based python project) requires an X11 session(for Pointcloud rendering with open3d).
If you have wayland you can try to do the following(warning: your interface might get significantly different but you can still run the project)
```bash
sudo startx
```

